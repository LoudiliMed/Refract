# setup.cfg — liste les modules plats de src/ à inclure dans le package
# (setuptools TOML ne supporte pas py_modules directement)
[options]
py_modules =
    ast_extractor
    cache_injector
    mcp_optimizer
    mcp_signal_check
    refract_cli
    refract_proxy
    token_counter
